name: Manual
on:
    workflow_dispatch:
        inputs:
            type: string
            default: Hello
            required: true
            description: A cheerful word
Jobs:
    run:
        runs-on: ubuntu-latest
        steps: 
            - run: echo "${{ input.greeting }}"